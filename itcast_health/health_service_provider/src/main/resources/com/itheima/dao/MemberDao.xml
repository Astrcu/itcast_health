<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itheima.dao.MemberDao">

    <insert id="add" parameterType="member" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO `t_member` (`fileNumber`, `name`, `sex`, `idCard`, `phoneNumber`, `regTime`)
        VALUES (#{fileNumber}, #{name}, #{sex}, #{idCard}, #{phoneNumber}, #{regTime});
    </insert>


    <select id="findByTelephone" parameterType="string" resultType="member">
        select * from t_member where  phoneNumber = #{telephone}
    </select>

    <select id="findCountByMonth" parameterType="string" resultType="long">
        select count(0) from t_member where regTime &lt;= #{month}
    </select>

    <select id="findTodayNewMember" parameterType="string" resultType="long">
        select count(0) from t_member where regTime = #{reportDate}
    </select>

    <select id="findTotalCount"  resultType="long">
        select count(0) from t_member
    </select>

    <select id="findCountByAfterDate" resultType="long">
        select count(0) from t_member where regTime &gt;= #{thisWeekMonday}
    </select>

</mapper>